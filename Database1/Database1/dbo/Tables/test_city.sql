CREATE TABLE [dbo].[test_city] (
    [city_id]     INT          NOT NULL,
    [city]        VARCHAR (50) NOT NULL,
    [country_id]  SMALLINT     NOT NULL,
    [last_update] TIME (7)     NOT NULL
);

