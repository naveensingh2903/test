CREATE TABLE [dbo].[test_actor] (
    [actor_id]    INT          NOT NULL,
    [first_name]  VARCHAR (45) NOT NULL,
    [last_name]   VARCHAR (45) NOT NULL,
    [last_update] TIME (7)     NOT NULL
);

